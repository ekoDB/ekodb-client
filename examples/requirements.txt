# Python dependencies for ekoDB examples
# NOTE: this file intentionally excludes ekodb_client itself — the examples are run
# against the locally built client (see the build-python-client target in the Makefile).
aiohttp>=3.8.0
websockets>=10.0
python-dotenv>=0.19.0
requests>=2.28.0
