{
  "name": "ekodb-client",
  "private": true,
  "workspaces": [
    "clients/nodejs"
  ],
  "scripts": {
    "build": "npm run build --workspace=clients/nodejs",
    "test": "npm run test --workspace=clients/nodejs",
    "clean": "npm run clean --workspace=clients/nodejs",
    "lint": "npm run lint --workspace=clients/nodejs",
    "lint:fix": "npm run lint:fix --workspace=clients/nodejs",
    "format": "npm run format --workspace=clients/nodejs"
  }
}